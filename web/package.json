{
  "name": "chviewer-web",
  "private": true,
  "scripts": {
    "build": "esbuild app.js --bundle --minify --format=iife --outfile=dist/bundle.js --loader:.ts=ts"
  },
  "dependencies": {
    "h3-js": "^4.1.0",
    "maplibre-gl": "^6.9.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.12"
  }
}
